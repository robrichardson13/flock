{
  "name": "flock",
  "private": true,
  "license": "MIT",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "build": "bun run --cwd packages/web build && bun run scripts/gen-assets.ts",
    "test": "bun test",
    "typecheck": "bun run --cwd packages/web typecheck && tsc -p tsconfig.json --noEmit",
    "flock": "bun run packages/cli/src/main.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.6.0"
  }
}

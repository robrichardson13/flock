// Committed declaration for the build-output `assets.generated.ts` (gitignored — see
// scripts/gen-assets.ts). The rest of the CLI type-checks against this rather than the generated
// file, which does not exist in a plain checkout with no web build.
export declare const ASSETS: Record<string, string>;
